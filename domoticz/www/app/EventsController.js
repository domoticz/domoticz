define(['app'], function (app) {
	app.controller('EventsController', [ '$scope', '$location', '$http', '$interval', function($scope,$location,$http,$interval) {
		init();

		function init()
		{
		};
	} ]);
});