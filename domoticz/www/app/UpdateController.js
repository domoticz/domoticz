define(['app'], function (app) {
	app.controller('UpdateController', [ '$scope', '$location', '$http', '$interval', function($scope,$location,$http,$interval) {
		init();

		function init()
		{
		};
	} ]);
});