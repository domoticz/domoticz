define(['app'], function (app) {
	app.controller('RoomplanController', [ '$scope', '$location', '$http', '$interval', function($scope,$location,$http,$interval) {
		init();

		function init()
		{
		};
	} ]);
});