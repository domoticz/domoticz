-- this scripts holds all the globally persistent variables and helper functions
-- avaible in your scripts

-- use like this in your scripts:
-- domoticz.globalData.myGlobalPersistentVar = 40
-- domoticz.helpers.myHelper()

-- NOTE:
-- THERE CAN BE ONLY ONE global_data SCRIPT

return {
	data = {
		myGlobalPersistentVar = { initial = 10 },
		myGlobalPersistentVarWithHistory = { history = true, maxItems = 10 },
	},
	helpers = {
		myHelper = function()
			-- code
		end
	}
}