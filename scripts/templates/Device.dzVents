return {
	active = false,
	on = {
		'myDevice'
	},
	data = {},
	execute = function(domoticz, device, info)
		domoticz.log('Device ' .. device.name .. ' was changed', domoticz.LOG_INFO)
		-- code
	end
}